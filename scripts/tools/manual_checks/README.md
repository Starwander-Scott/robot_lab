# Manual Checks

This directory stores one-off inspection scripts and temporary validation helpers
that were previously kept in the repository root.

These files are not part of the normal training, playback, or package entrypoints.
They are kept only as manual references for MuJoCo and policy debugging work.

Prefer the maintained scripts under `scripts/mujoco/` and `scripts/tools/` for
regular usage.
